import { SummaryLimitPipe } from './summary-limit.pipe';

describe('SummaryLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
